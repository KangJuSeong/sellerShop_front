import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    dateContainer: {
      flexDirection: "row",
    },
    dateText: {
      fontSize: 24,
      fontStyle: "normal",
      letterSpacing: 0,
      color: "#252631"
    },
    textBold: {
      fontWeight: "bold",
    }
  });

export default styles;